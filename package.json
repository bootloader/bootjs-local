{
  "name": "@bootloader/local",
  "version": "1.0.0",
  "description": "local development server with ngrok and ndoemon",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@bootloader/config": "^1.4.0",
    "@ngrok/ngrok": "^0.6.0",
    "nodemon": "^3.0.1",
    "cross-env": "^7.0.3"
  }
}
